# 🚀 OpenWrt Universal Builder (Image + Source)

Наверняка многие хотели иметь персональный бинарник роутера на OpenWrt с полным конфигом и кастомными пакетами. Не просто резервная копия, а прямой откат версии в чистый `.bin`!

Теперь этот проект вырос в **Универсальный комбайн**, объединяющий два подхода: быстрая сборка (**ImageBuilder**) и полная компиляция (**SourceBuilder**).

В версии 3.0+ произошел **глобальный рефакторинг**: старые скрипты `_Image_Builder.bat` и `_Source_Builder.bat` были удалены и заменены единым `_Builder.bat`, который управляет обоими режимами.

---

## 📥 Установка и Скачивание (Download)

Весь проект — это **один самораспаковывающийся файл** `_unpacker.bat`.
Вам не нужно качать zip-архивы или клонировать репозиторий. Просто скачайте этот файл, положите в **пустую папку** (без кириллицы в пути) и запустите.

*   **Подготовка:** [скачайте и установите Docker Desktop](https://desktop.docker.com/win/main/amd64/Docker%20Desktop%20Installer.exe), после чего убедитесь в отсутствии ошибок.

Выберите удобный способ:

### 🔹 Способ 1: Стабильные релизы (Рекомендуется)
Проверенные версии с описанием изменений.
👉 **[Перейти в раздел Releases](https://github.com/iqubik/routerFW/releases)**

### 🔹 Способ 2: Актуальная версия (Bleeding Edge)
Самая свежая версия кода прямо из ветки `main`. Содержит последние правки и функции.

👉 **[Скачать _unpacker.bat (RAW)](https://github.com/iqubik/routerFW/raw/main/_unpacker.bat)**

> **Как запустить:**
> 1. Скачайте `_unpacker.bat`.
> 2. Создайте папку, например `C:\OpenWrtBuilder`.
> 3. Поместите файл туда и запустите. Он сам развернет все необходимые файлы (`_Source_Builder.bat`, профили и т.д.).
> 4. Запустите `_Builder.bat` - можно пользоваться.

---

## 🖥 Интерфейс

```text
=================================================================
 OpenWrt Builder v3.х
 Текущий режим: [SOURCE BUILDER (Полная компиляция)]
=================================================================

   [1] xiaomi_4a_gigabit.conf
   [2] keenetic_giga.conf

   [A] Собрать ВСЕ (Параллельно)
   [M] Переключить режим на IMAGE
   [C] CLEAN / MAINTENANCE (Очистка кэша)
   [W] Profile Wizard (Создать профиль)
   [0] Выход
```

---

## ⚡ О проекте

Это конвейер по сборке прошивок в **Docker контейнерах** (требуется Docker Desktop под Windows 10/11). Система полностью изолирована от вашей Windows, не мусорит в реестре и удаляется одним кликом.

### Два режима работы:

#### 1. 🐇 Image Builder (Быстрый)
*   **Суть:** Берет готовые пакеты с серверов OpenWrt и перепаковывает их в прошивку.
*   **Скорость:** 1-2 минуты.
*   **Для чего:** Добавить пакеты (`luci`, `vpn`), зашить Wi-Fi/PPPoE конфиги, убрать лишнее.
*   **Особенности:**
    *   **Atomic Downloads:** Умная защита от сбоев сети. Скачивание SDK происходит во временные файлы, что позволяет запускать **параллельную сборку** нескольких профилей без конфликтов в изолированных контейнерах.
    *   **Умный кэш:** Повторная сборка занимает ~30 секунд.
    *   **Функционал:**
        *   Интеграция `.ipk` файлов, которых нет в репозитории (папка `custom_packages`).
        *   Внедрение бэкапов и конфигов "из коробки" (папка `custom_files`).

#### 2. 🐢 Source Builder (Мощный)
*   **Суть:** Компилирует прошивку **из исходного кода** (C/C++) с нуля.
*   **Скорость:** Первый запуск ~20-60 минут (сборка Toolchain), повторные ~3-5 минут (благодаря **CCache**).
*   **Для чего:** Нестандартный размер разделов (16MB мод), патчи ядра, сборка программ, которых нет в `opkg`.
*   **Особенности:**
    *   **🚀 CCache (Кэш компилятора):** Встроенная система кэширования ускоряет повторные сборки в 10 раз.
    *   **Smart Hooks & Vermagic:** Автоматически подменяет "отпечаток" ядра, позволяя устанавливать официальные `kmod` пакеты на кастомную прошивку.
    *   **Persistence:** Создает персональные Docker-тома для каждого профиля.
    *   **Git-контроль:** Работает с ветками (Branches) и тегами (Tags).

---

## 🧹 Система очистки и обслуживания (Maintenance Mode)

В версии 3.0 внедрен профессиональный мастер очистки (`[C] CLEAN` в меню), позволяющий управлять дисковым пространством и решать проблемы сборки без удаления всего подряд.

Система работает с кэшами **гранулярно** (точечно):

| Опция | Описание | Когда использовать? |
| :--- | :--- | :--- |
| **Soft Clean** | Выполняет `make clean` внутри контейнера. | Если сборка падает с ошибками линковки, но вы не хотите пересобирать Toolchain (экономит 40 мин). |
| **Hard Reset** | Удаляет рабочую папку (`src-workdir`), но **сохраняет** скачанные исходники (`dl`). | Если вы сломали Toolchain или Git-репозиторий. Следующая сборка начнется с компиляции инструментов. |
| **Clean DL** | Удаляет архив исходных кодов (`dl`). | Если нужно освободить место на диске или исходники скачались битыми. |
| **Clean Ccache** | Сбрасывает кэш компилятора. | Если вы сменили архитектуру процессора или версию GCC. |
| **Factory Reset** | Удаляет **ВСЁ** для выбранного профиля. | Чтобы вернуть проект в состояние "как после установки". |

> **💡 Умный поиск:** Мастер умеет чистить мусор как для **одного конкретного профиля**, так и проводить глобальную зачистку **[A] ALL** для всех старых контейнеров сразу.

---

## ✍️ Мастер создания профилей (Profile Wizard)

Забыли параметры своего роутера или боитесь ошибиться при ручном создании `.conf` файла? Используйте пункт меню `[W] Profile Wizard`.

Это **интерактивный пошаговый мастер**, который защищает от ошибок и упрощает создание конфигураций:

*   **Надежность:** Мастер проверяет каждый ваш ввод (например, номер пункта меню) и не дает продолжить, пока не будут введены корректные данные. Это полностью исключает ошибки из-за опечаток.
*   **Удобный интерфейс:** На каждом шаге вы видите, что уже было выбрано (`Release > Target > Subtarget...`), что помогает не терять контекст.
*   **Самодокументируемые конфиги:** По завершении мастер создает `.conf` файл, наполненный **полезными комментариями и примерами** для продвинутых опций (`SRC_EXTRA_CONFIG`, кастомные репозитории и т.д.), что сильно упрощает их дальнейшую настройку.

---

## 📂 Структура папок

Все папки создаются автоматически при первом запуске (благодаря `_unpacker.bat`).

*   `profiles/` — **Единый центр управления.** Универсальные `.conf` файлы.
*   `custom_files/имя_профиля/` — Файловый оверлей. Всё, что лежит здесь, попадет в прошивку (например, `/etc/config/network`).
    *   *Автоматика:* Генерируется скрипт прав доступа (`99-permissions.sh`), чтобы файлы с Windows корректно работали в Linux.
*   `custom_packages/` — (Для **ImageBuilder**) Папка для готовых файлов `.ipk`.
*   `src_packages/` — (Для **SourceBuilder**) Папка для **исходных кодов** (папки с `Makefile`).
*   `firmware_output/` — Склад готовых прошивок.
    *   Внутри папок с датой теперь сохраняется файл `build.config` — точная копия конфигурации, с которой была собрана эта версия.
*   `scripts/` — Папка с полезными скриптами.

---

## ⚙️ Универсальный Профиль

Один файл конфигурирует оба режима. Пример (`profiles/universal_example.conf`):

```bash
# === ОБЩИЕ НАСТРОЙКИ ===
PROFILE_NAME="my_router_universal"

# Общий список пакетов.
# Не нужно писать kernel, dnsmasq, firewall - они сами подтянутся.
# Пишем только свои "хотелки" (luci) или "удалялки" (минус перед именем).
COMMON_LIST="luci uhttpd openssh-sftp-server htop -wpad-basic wpad-mbedtls"

# ==========================================
# ЧАСТЬ 1: IMAGE BUILDER
# ==========================================
IMAGEBUILDER_URL="https://downloads.openwrt.org/releases/24.10.0/targets/ramips/mt7621/openwrt-imagebuilder-..."
TARGET_PROFILE="xiaomi_mi-router-4a-gigabit"
# EXTRA_IMAGE_NAME="fast-v1"
PKGS="$COMMON_LIST"

# ==========================================
# ЧАСТЬ 2: SOURCE BUILDER
# ==========================================
SRC_REPO="https://github.com/openwrt/openwrt.git"
SRC_BRANCH="v24.10.0" # Используйте ТЕГИ для совпадения версий ядра!

# Параметры архитектуры (из OpenWrt Table of Hardware)
SRC_TARGET="ramips"
SRC_SUBTARGET="mt7621"
SRC_PACKAGES="$COMMON_LIST"

# Кастомные репозитории (Ключ + URL)
CUSTOM_KEYS="https://fantastic-packages.github.io/releases/24.10/53ff2b6672243d28.pub"
CUSTOM_REPOS="src/gz fantastic_luci https://fantastic-packages.github.io/releases/24.10/packages/mipsel_24kc/luci
src/gz fantastic_packages https://fantastic-packages.github.io/releases/24.10/packages/mipsel_24kc/packages
src/gz fantastic_special https://fantastic-packages.github.io/releases/24.10/packages/mipsel_24kc/special"

# Тонкая настройка конфига (v2.1+)
# Позволяет менять размеры разделов или отключать опции ядра до компиляции
SRC_EXTRA_CONFIG="CONFIG_TARGET_KERNEL_PARTSIZE=64 \
CONFIG_TARGET_ROOTFS_PARTSIZE=256"
```

---

## 🪝 Smart Hooks и Vermagic Hack

В комплекте идет умный скрипт `scripts/hooks.sh` (версия 1.2+).
При помещении в папку `custom_files/ваш_профиль/` он активирует две мощные функции:

1.  **Git-Safe Patching:** Безопасное изменение исходного кода перед сборкой. Позволяет менять DTS, Makefiles (например, для 16MB мода). Автоматические бэкапы защищают от поломки дерева исходников при смене версий.
2.  **Vermagic Hack (Совместимость с репозиторием):**
    *   *Проблема:* Обычно, когда вы собираете свое ядро (SourceBuilder), его "отпечаток" (vermagic) отличается от официального. Из-за этого вы не можете установить пакеты `kmod-*` (драйверы) из официального репозитория.
    *   *Решение:* Скрипт **автоматически скачивает официальный манифест** для вашей версии OpenWrt, выдергивает оттуда правильный хэш и "обманывает" систему сборки.
    *   *Итог:* Вы получаете кастомную прошивку, на которую **можно ставить официальные драйверы** (USB, Wireguard, FS) без ошибок!
    > **💡 Логика "самовосстановления":** Модуль SourceBuilder обладает **встроенным механизмом отката**.
> **Сценарий:** Вы использовали профиль с `hooks.sh` (для Vermagic или других патчей), а затем убрали этот хук.
> **Результат:** Система сама обнаружит "грязное" состояние сборки и проведет **полную автоматическую очистку**: восстановит оригинальные файлы сборки, выполнит глубокую очистку кэша ядра и **полностью сбросит CCACHE**. Это предотвращает труднодиагностируемые ошибки и делает систему гораздо более надежной.

---

## 🎛️ Интерактивная настройка (Menuconfig)

В версии 3.3 появилась возможность тонкой настройки ядра и пакетов через стандартное графическое меню OpenWrt (`make menuconfig`). Это доступно только в режиме **Source Builder**.

### Как это работает:

1.  **Запуск меню:** В главном меню выберите пункт `[K] MENUCONFIG`.
3.  **Настройка:** Если это первый запуск то после подготовки откроется классический синий интерфейс Linux Kernel Configuration. Выберите нужные модули ядра, драйверы или пакеты.
4.  **Сохранение:** Нажмите `<Save>` (оставьте имя `.config`) и `<Exit>`.
5.  **Результат:** Скрипт перехватит конфиг и сохранит его как **мастер-файл** в папку:
    `firmware_output/sourcebuilder/<имя_профиля>/manual_config`

### ⚠️ Приоритет конфигурации (Manual Config)

Система сборки теперь автоматически определяет стратегию конфигурации:

*   **Автоматический режим (по умолчанию):** Если файла `manual_config` нет в папке выходных файлов с прошивками, сборщик генерирует `.config` на лету, используя переменные из вашего профиля (`SRC_PACKAGES`, `SRC_EXTRA_CONFIG`, `COMMON_LIST`).
*   **Ручной режим (Приоритетный):** Если файл `manual_config` обнаружен в папке вывода прошивок, сборщик **ИГНОРИРУЕТ** списки пакетов и опции ядра из `.conf` файла. Вместо этого он применяет ваш сохраненный конфиг "как есть" (добавляя только настройки кэширования).

> **Как вернуться к авто-сборке?** Просто удалите или переименуйте файл `manual_config` в папке `firmware_output`.

---

## ⚠️ ВАЖНО: Ошибка "Package not found"

Некоторые примеры профилей (например, `ZBT` или сборки с `Zapret/Iperf3`) настроены на максимальный функционал и **не соберутся "из коробки"**.

**Причина:** В них указаны пакеты, которые мы не можем распространять (лицензия) или которые нужно скачивать отдельно.
**Решение:**
1.  Либо найдите недостающие `.ipk` (например, `zapret`, `luci-app-zapret`) и положите их в папку `custom_packages`.
2.  Либо откройте профиль и **удалите** названия этих пакетов из списка `COMMON_LIST`.

---

## 🛠 Полезные советы (FAQ)

### Q: ImageBuilder или SourceBuilder? Что выбрать?
**A:**
*   Просто добавить LuCI, VPN и настроить Wi-Fi? → **ImageBuilder** (1-2 минуты).
*   Нужен 16MB мод, нестандартное ядро, вырезать лишнее для 4MB флешки или драйвер, которого нет в репо? → **SourceBuilder** (20+ минут).

### Q: У меня параллельная сборка (пункт [A]), не будут ли конфликты?
**A:** Нет. В версии 3.0 внедрена система **Atomic Downloads** и **Global Locks**. Если 5 профилей используют один и тот же ImageBuilder SDK, скачает его только первый, а остальные будут ждать завершения. Ошибки "файл занят" или битые архивы исключены.

### Q: Сборка упала с ошибкой сети (wget error 4), интернет нестабильный.
**A:** Билдер использует **Smart Retries**. Внутри контейнера настроен глобальный `wgetrc`, который делает 5 попыток при разрыве соединения. Если вся сборка упадет из-за сети, скрипт автоматически перезапустит её (до 3 раз).

### Q: Как проверить, сработал ли Vermagic Hack?
**A:**
1. Прошейте роутер.
2. Попробуйте установить любой драйвер: `opkg install kmod-fs-btrfs`.
3. Если установилось без ошибок `checksum mismatch` — поздравляем, у вас "официальное" ядро!

### Q: Как сделать 16MB мод (Hardware Mod)?
**A:** Используйте **Hooks**:
1.  Создайте файл `custom_files/ваш_профиль/hooks.sh`.
2.  Пропишите в нем `sed` команды для замены адресов в `.dts` и размера в `Makefile`.
3.  При запуске SourceBuilder применит патч к исходникам автоматически.
4.  Образец такого кода лежит в `/scripts/hooks.sh`

### Q: Ошибка зависимостей (dnsmasq vs dnsmasq-full)
**A:** Стандартная проблема. Один пакет требует `dnsmasq`, а вы ставите `dnsmasq-full`.
**Решение:** В списке пакетов явно удалите стандартный и добавьте полный:
`PKGS="-dnsmasq dnsmasq-full ..."`

---

## 🔧 Полезные скрипты (папка `scripts`)

*   `scripts\etc\uci-defaults\99-permissions.sh` — Скрипт решающий проблему прав доступа dropbear SSH (автоматически создаётся в папках /custom_files)
*   `scripts\hooks.sh` — Пример Pre-build Hook с патчем VerMagic.
##  Дополнительно:
*   `scripts\diag.sh` — Создает обезличенный отчет о системе для общей диагностики устройств с помощью ИИ.
*   `scripts\upgrade.sh` — Аналог `apt upgrade` для роутера (обновляет все пакеты).
*   `scripts\packager.sh` — Скрипт позволяющий получить список пакетов установленных в системе отфильтровав autoinstalled.
