# 🚀 OpenWrt Universal Builder (Image + Source)

Наверняка многие хотели иметь персональный бинарник роутера на OpenWrt с полным конфигом и кастомными пакетами. Не просто резервная копия, а прямой откат версии в чистый `.bin`!

Теперь этот проект вырос в **Универсальный комбайн**, объединяющий два подхода: быстрая сборка (**ImageBuilder**) и полная компиляция (**SourceBuilder**).

---

## ⚡ О проекте

Это конвейер по сборке прошивок в **Docker контейнерах** (требуется Docker Desktop под Windows 10/11). Система полностью изолирована от вашей Windows, не мусорит в реестре и удаляется одним кликом.

### Два режима работы:

#### 1. 🐇 Image Builder (Быстрый)
*   **Суть:** Берет готовые пакеты с серверов OpenWrt и перепаковывает их в прошивку.
*   **Скорость:** 1-2 минуты.
*   **Для чего:** Добавить пакеты (`luci`, `vpn`), зашить Wi-Fi/PPPoE конфиги, убрать лишнее.
*   **Особенности:**
    *   **Универсальность:** Поддерживает как старые версии (v19.x), так и современные (v24.x) благодаря разным Docker-образам.
    *   **Умный кэш:** Повторная сборка занимает ~30 секунд.
    *   **Функционал:**
        *   Интеграция `.ipk` файлов, которых нет в репозитории (папка `custom_packages`).
        *   Внедрение бэкапов и конфигов "из коробки" (папка `custom_files`).
        *   Подключение внешних репозиториев (без скачивания файлов).

#### 2. 🐢 Source Builder (Мощный)
*   **Суть:** Компилирует прошивку **из исходного кода** (C/C++) с нуля.
*   **Скорость:** Первый запуск ~20-60 минут (сборка Toolchain), повторные ~2-5 минут (инкрементальная сборка).
*   **Для чего:** Нестандартный размер разделов (16MB мод), патчи ядра, сборка программ, которых нет в `opkg`.
*   **Особенности:**
    *   **Pre-build Hooks:** Поддержка скриптов `hooks.sh`. Позволяет автоматически патчить исходный код (Makefiles, DTS, C-код) прямо перед началом сборки.
    *   **Persistence (Живой кэш):** Создает персональные Docker-тома для каждого профиля. Если вы меняете один пакет, пересобирается *только он*, а не вся прошивка.
    *   **Двухрежимность:** Автоматически подбирает среду: Ubuntu 18.04 + Python 2 (для старых OpenWrt) или Ubuntu 22.04 + Python 3 (для новых).
    *   **Надежность:** Встроена защита от падения `git.openwrt.org` (авто-переключение на GitHub).
    *   **Git-контроль:** Работает с ветками (Branches) и тегами (Tags).

---

## 🖥 Интерфейс и Запуск

Запуск осуществляется через `.bat` файлы. Билдеры связаны между собой — вы можете переключаться прямо из меню.

> **Важно для Windows пользователей:**
> 1. Установите **Docker Desktop**.
> 2. Запустите его (пусть висит в трее).
> 3. Запускайте `_Source_Builder.bat` или `_Image_Builder.bat`.
> **Больше ничего делать не надо.** Скрипты сами скомандуют Докеру скачать Ubuntu, настроить среду и отдать вам файл.

```text
========================================
 OpenWrt SOURCE Builder
========================================

Обнаруженные Source-профили:
[1] giga_24105_main_full.conf
[2] tplink_841n_v9_190710_full.conf
...

[A] Собрать ВСЕ профили (Параллельно)
[S] Переключиться на IMAGE Builder
[0] Выход
```

---

## 📂 Структура папок

Все папки создаются автоматически при первом запуске (благодаря `_unpacker.bat`).

*   `profiles/` — **Единый центр управления.** Универсальные `.conf` файлы.
*   `custom_files/имя_профиля/` — Файловый оверлей. Всё, что лежит здесь, попадет в прошивку (например, `/etc/config/network`).
    *   *Автоматика:* Генерируется скрипт прав доступа (`99-permissions.sh`), чтобы файлы с Windows корректно работали в Linux.
    *   **Hooks:** Если положить сюда файл `hooks.sh`, он будет исполнен SourceBuilder'ом перед сборкой (для патчинга исходников).
*   `custom_packages/` — (Для **ImageBuilder**) Папка для готовых файлов `.ipk`.
*   `src_packages/` — (Для **SourceBuilder**) Папка для **исходных кодов** (папки с `Makefile`).
*   `firmware_output/` — Склад готовых прошивок.
    *   `imagebuilder/` — результаты быстрой сборки.
    *   `sourcebuilder/` — результаты компиляции (включая buildinfo, manifest и json).

---

## ⚙️ Универсальный Профиль

Один файл конфигурирует оба режима. Пример (`profiles/universal_example.conf`):

```bash
# === ОБЩИЕ НАСТРОЙКИ ===
PROFILE_NAME="my_router_universal"

# Общий список пакетов.
# Не нужно писать kernel, dnsmasq, firewall - они сами подтянутся.
# Пишем только свои "хотелки" (luci) или "удалялки" (минус перед именем).
COMMON_LIST="luci uhttpd openssh-sftp-server htop -wpad-basic wpad-mbedtls"

# ==========================================
# ЧАСТЬ 1: IMAGE BUILDER
# ==========================================
IMAGEBUILDER_URL="https://downloads.openwrt.org/releases/24.10.0/targets/ramips/mt7621/openwrt-imagebuilder-..."
TARGET_PROFILE="xiaomi_mi-router-4a-gigabit"
# EXTRA_IMAGE_NAME="fast-v1"
PKGS="$COMMON_LIST"

# ==========================================
# ЧАСТЬ 2: SOURCE BUILDER
# ==========================================
SRC_REPO="https://github.com/openwrt/openwrt.git"
SRC_BRANCH="v24.10.0" # Используйте ТЕГИ для совпадения версий ядра!

# Параметры архитектуры (из OpenWrt Table of Hardware)
SRC_TARGET="ramips"
SRC_SUBTARGET="mt7621"
SRC_PACKAGES="$COMMON_LIST"

# Кастомные репозитории (Ключ + URL)
CUSTOM_KEYS="https://fantastic-packages.github.io/releases/24.10/53ff2b6672243d28.pub"
CUSTOM_REPOS="src/gz fantastic_luci https://fantastic-packages.github.io/releases/24.10/packages/mipsel_24kc/luci
src/gz fantastic_packages https://fantastic-packages.github.io/releases/24.10/packages/mipsel_24kc/packages
src/gz fantastic_special https://fantastic-packages.github.io/releases/24.10/packages/mipsel_24kc/special"

# Тонкая настройка конфига (v2.1+)
# Позволяет менять размеры разделов или отключать опции ядра до компиляции
SRC_EXTRA_CONFIG="CONFIG_TARGET_KERNEL_PARTSIZE=64 \
CONFIG_TARGET_ROOTFS_PARTSIZE=256"
```

---

## 🪝 Pre-build Hooks и Vermagic - hooks.sh

В SourceBuilder встроен **Хук (Hook)**. Это скрипт, который позволяют модифицировать исходный код OpenWrt «на лету» прямо перед началом компиляции.

### Зачем это нужно?
Раньше, чтобы изменить карту разделов (16MB mod), разблокировать частоты Wi-Fi или поправить кривой драйвер, вам приходилось делать **Fork** репозитория OpenWrt, править код, коммитить изменения и собирать со своего Git.

Теперь достаточно положить файл `/script/hooks.sh` в папку `/custom_files/ВАШ_ПРОФИЛЬ/`.
Скрипт запустится внутри контейнера в корне исходников.
*   **Пример:** `sed` команда, которая меняет `4MB` на `16MB` в файле `.dts`.
*   **Пример:** `curl`, который скачивает патч и применяет его через `patch -p1`.

### 🧩 Pre-build Hooks и Vermagic (Совместимость пакетов)

Использование `hooks.sh` и `SRC_EXTRA_CONFIG` дает огромную гибкость, но вы должны понимать, как это влияет на репозиторий пакетов.

**1. Обычные пакеты (Userspace) — ✅ ВСЁ ОК**
Программы вроде `luci`, `curl`, `youtube-dl`, `vpn-policy-routing` **не зависят** от версии ядра. Они будут устанавливаться и обновляться из официальных репозиториев без проблем, даже если вы пересобрали ядро с другими опциями.

**2. Модули ядра (`kmod-*`) — ⚠️ ЕСТЬ НЮАНСЫ**
У ядра Linux есть "отпечаток пальца" — **Vermagic**. Он меняется, если вы через `SRC_EXTRA_CONFIG` затронули конфигурацию ядра (например, включили поддержку FPU или изменили планировщик).

*   **Если Vermagic совпадает:** Официальные `kmod-*` ставятся без проблем.
*   **Если Vermagic изменился:** При попытке установить `kmod` из репозитория `opkg` выдаст ошибку `kernel version checksum mismatch`.

**Как с этим жить:**
1.  **Лучший вариант:** Сразу добавьте все нужные драйверы (USB, файловые системы, WireGuard) в список пакетов профиля (`SRC_PACKAGES`). Они соберутся вместе с ядром и будут работать идеально.
2.  **Вариант для смелых:** Если очень нужно доустановить модуль позже, можно использовать `opkg install --force-vermagic`, но стабильность работы драйвера в этом случае зависит от того, насколько сильно ваши правки отличаются от стока.

## ⚠️ ВАЖНО: Ошибка "Package not found"

Некоторые примеры профилей (например, `ZBT` или сборки с `Zapret/Iperf3`) настроены на максимальный функционал и **не соберутся "из коробки"**.

**Причина:** В них указаны пакеты, которые мы не можем распространять (лицензия) или которые нужно скачивать отдельно.
**Решение:**
1.  Либо найдите недостающие `.ipk` (например, `zapret`, `luci-app-zapret`) и положите их в папку `custom_packages`.
2.  Либо откройте профиль и **удалите** названия этих пакетов из списка `COMMON_LIST`.

---

Принято. Это важное упрощение, которое убирает путаницу.

Вот исправленный блок **FAQ**, где убрано упоминание `SRC_DEVICE` и отражена унификация параметра `TARGET_PROFILE`.

Замените старую секцию **🛠 Экспертные советы (FAQ)** на эту версию:

---

## 🛠 Экспертные советы (FAQ)

### Q: ImageBuilder или SourceBuilder? Что выбрать?
**A:**
*   Просто добавить LuCI, VPN и настроить Wi-Fi? → **ImageBuilder** (1-2 минуты).
*   Нужен 16MB мод, нестандартное ядро, вырезать лишнее для 4MB флешки или драйвер, которого нет в репо? → **SourceBuilder** (20+ минут).

### Q: Нужно ли мне настраивать Linux или WSL вручную?
**A:** **Нет.** Вам нужно только установить Docker Desktop.
Скрипты сами развернут нужную версию Linux (Ubuntu 18.04 или 22.04) внутри контейнера, настроят Python и библиотеки. Результат сборки автоматически появится в папке `firmware_output` в Windows. Никаких команд в консоли Linux вводить не нужно.

### Q: Как полностью очистить сборку (сбросить кэш)?
**A:** SourceBuilder создает отдельный Docker-том для каждого профиля.
Самый простой способ начать "с чистого листа" — **изменить `PROFILE_NAME`** в файле конфига (например, добавить `_v2`). Сборщик увидит новое имя и создаст чистое окружение заново.

### Q: Какой параметр отвечает за модель устройства?
**A:** Теперь используется **единый параметр `TARGET_PROFILE`**.
Унифицированная конфигурация: это имя автоматически подставляется и в аргументы ImageBuilder, и в настройки SourceBuilder (для выбора Target Profile в `.config`). Дублировать названия больше не нужно - используем только `TARGET_PROFILE`.

### Q: Скрипт `hooks.sh` нужно делать исполняемым (`chmod +x`)?
**A:** Нет, это "полный автопилот". Сборщик сам найдет файл в папке `custom_files`, выдаст ему права на исполнение и запустит внутри контейнера в нужный момент (перед компиляцией).

### Q: Как сделать 16MB мод (Hardware Mod)?
**A:** Раньше требовался форк репозитория. Теперь используйте **Hooks**:
1.  Создайте файл `custom_files/ваш_профиль/hooks.sh`.
2.  Пропишите в нем `sed` команды для замены адресов в `.dts` и размера в `Makefile`.
3.  При запуске SourceBuilder применит патч к исходникам автоматически.

### Q: Ошибка зависимостей (dnsmasq vs dnsmasq-full)
**A:** Стандартная проблема. Один пакет требует `dnsmasq`, а вы ставите `dnsmasq-full`.
**Решение:** В списке пакетов явно удалите стандартный и добавьте полный:
`PKGS="-dnsmasq dnsmasq-full ..."`

### Q: Source Builder долго висит на `make[3] ... compile`
**A:** При повторном запуске это нормально. Система проверяет тысячи файлов на изменения. Это занимает 1-2 минуты, но тяжелой нагрузки на процессор в этот момент нет.

### Q: Как добавить свою программу (исходники) в Source Builder?
**A:** Найдите папку с исходным кодом программы (где лежит `Makefile`) и просто скопируйте её в папку `src_packages`. Скрипт сам внедрит её в дерево сборки OpenWrt.

---

## 🔧 Полезные скрипты (папка `scripts`)

*   `scripts\hooks.sh` — 1. Пример Pre-build Hook. Содержит безопасный код для модификации исходников. 2. Содержит патч VerMagic для подписи своих сорс-сборок как официальных.
*   `scripts\diag.sh` — Создает обезличенный отчет о системе (пароли скрываются) для помощи на форумах или анализа ИИ.
*   `scripts\upgrade.sh` — Аналог `apt upgrade` для роутера (обновляет все пакеты).
*   `create_profile.ps1` — Помощник для создания профиля (парсит сайт OpenWrt и создает универсальный конфиг).

Вот блок, который нужно разместить в самом верху `README.md`, сразу после заголовка и описания проекта. Он максимально четко направляет пользователя.

***

## 📥 Установка и Скачивание (Download)

Весь проект — это **один самораспаковывающийся файл** `_unpacker.bat`.
Вам не нужно качать zip-архивы или клонировать репозиторий. Просто скачайте этот файл, положите в **пустую папку** (без кириллицы в пути) и запустите.

Выберите удобный способ:

### 🔹 Способ 1: Стабильные релизы (Рекомендуется)
Проверенные версии с описанием изменений.
👉 **[Перейти в раздел Releases](https://github.com/iqubik/routerFW/releases)**

### 🔹 Способ 2: Актуальная версия (Bleeding Edge)
Самая свежая версия кода прямо из ветки `main`. Содержит последние правки и функции.
👉 **[Скачать _unpacker.bat (RAW)](https://github.com/iqubik/routerFW/raw/main/_unpacker.bat)**

> **Как запустить:**
> 1. Скачайте `_unpacker.bat`.
> 2. Создайте папку, например `C:\OpenWrtBuilder`.
> 3. Поместите файл туда и запустите. Он сам развернет все необходимые файлы (`_Source_Builder.bat`, профили и т.д.).