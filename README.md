Вот обновленная документация, которая объединяет оба инструмента в одну экосистему, описывает новые папки, логику универсальных профилей и особенности компиляции из исходников.

***

# OpenWrt Universal Builder (Image + Source)

Наверняка многие хотели иметь персональный бинарник роутера на OpenWrt с полным конфигом и кастомными пакетами. Не просто резервная копия, а прямой откат версии в `.bin`!

Теперь этот проект вырос в **Универсальный комбайн**, объединяющий два подхода: быстрая сборка (ImageBuilder) и полная компиляция (SourceBuilder).

## 🚀 О проекте

Это конвейер по сборке прошивок в **Docker контейнерах** (требуется Docker Desktop под Windows 10/11). Система полностью изолирована от вашей Windows и не засоряет её.

### Два режима работы:

> **Примечание:** Не пугайтесь `base64` вставок в коде — их можно расшифровать онлайн и убедиться, что всё безопасно.

1.  **⚡ Image Builder (Быстрый)**
    *   **Суть:** Берет готовые пакеты с серверов OpenWrt и упаковывает их в прошивку.
    *   **Скорость:** 1-2 минуты.
    *   **Для чего:** Добавить пакеты, зашить конфиги, убрать лишнее, изменить размер разделов.
    *   **Особенности:**
        *   **Универсальность:** Собирает как старые версии (v19 на спец. докер-образе в старом окружении), так и современные (v24 в свежем).
        *   **Скорость:** Все сборки идут с умным кэшированием.
            *   Время повторной сборки: ~30 секунд.
            *   Время первичной сборки: до 2 минут (плюс ~5 минут на первый старт окружения).
        *   **Автономность:** Система сборки находится в одном `.bat` файле.
        *   **Функционал:** Реализован полный функционал ImageBuilder:
        *   0. Относительно просто быстро и чисто собрать свою прошивку.
        *   1. Зашить свои ipk в прошивку (экономим место)
        *   2. Зашить свои backup конфиги в сборку (заранее настроенная прошивка)
        *   3. Подключить внешние репозитории пакетов и ставить из них пакеты (можно и не качать ipk если есть внешнее репо)
        *   4. Менять размеры разделов (rootFS и kernelFS), давать дополнительное спец имя выходному файлу.
        *   5. Отключать сервисы, которые должны быть деактивированы в прошивке.

2.  **🏭 Source Builder (Мощный)**
    *   **Суть:** Компилирует прошивку **из исходного кода** (C/C++) с нуля.
    *   **Скорость:** Первый запуск ~20-50 минут (сборка Toolchain и инструментов), повторные ~2-5 минут (инкрементальная сборка).
    *   **Для чего:** Когда нужна нестандартная конфигурация ядра, специфичные патчи, полная независимость от репозиториев или сборка пакетов, которых нет в официальном доступе.
    *   **Особенности:**
        *   **Умное Кэширование (Persistence):** Создает персональные Docker-тома для каждого профиля. Сохраняет рабочую директорию (`build_dir`) и скачанные исходники (`dl`). Если вы меняете один пакет, пересобирается *только он*, а не вся прошивка.
        *   **Двухрежимность:** Автоматически определяет версию OpenWrt и подбирает нужную среду:
            *   *Legacy:* Ubuntu 18.04 + Python 2 (для версий 18.06/19.07).
            *   *Modern:* Ubuntu 22.04 + Python 3 (для версий 21.02 — 24.x).
        *   **Функционал:**
        *   0. **Git-контроль:** Работает как с ветками (Branches) для свежих патчей, так и с тегами (Tags) для совпадения версий ядра (Vermagic).
        *   1. **Инъекция Исходников:** Если положить папку с исходным кодом программы (Makefile) в `src_packages`, билдер сам внедрит её в дерево сборки и скомпилирует.
        *   2. **Надежность сети:** Встроена защита от падения серверов OpenWrt (ошибка 503). Скрипт умеет на лету переключать загрузку фидов на зеркала GitHub.
        *   3. **Глубокий конфиг:** Поддерживает изменение размеров разделов (`ROOTFS`/`KERNEL`) и автоматическую генерацию `.config` на основе списка пакетов.
        *   4. **Файловый оверлей:** Как и в ImageBuilder, зашивает ваши конфиги и скрипты из `custom_files` прямо в образ.
        *   5. **Полный лог:** Сохраняет не только образы `.bin`, но и `build.config`, манифесты пакетов и контрольные суммы.

---

## 🖥 Интерфейс и Навигация

Запуск осуществляется через `.bat` файлы. Билдеры связаны между собой — вы можете переключаться прямо из меню.

```text
========================================
OpenWrt SOURCE Builder v1.2 (iqubik)
========================================

Обнаруженные Source-профили:
[1] giga_24105_main_full.conf
[2] tplink_841n_v9_190710_full.conf
...

[A] Собрать ВСЕ профили (Параллельно)
[S] Переключиться на IMAGE Builder
[0] Выход
```

---

## 📂 Структура папок

Все папки создаются автоматически при первом запуске.

*   `profiles/` — **Единый центр управления.** Здесь лежат универсальные `.conf` файлы, которые понимают оба билдера.
*   `custom_files/имя_профиля/` — Файловый оверлей. Всё, что лежит здесь, попадет в прошивку в те же пути (например, `/etc/config/network`).
    *   *Автоматика:* При создании папки генерируется скрипт `etc/uci-defaults/99-permissions.sh` для исправления прав доступа (Linux permissions) при сборке из-под Windows.
*   `custom_packages/` — (Для ImageBuilder) Папка для готовых файлов `.ipk`.
*   `src_packages/` — (Для SourceBuilder) Папка для **исходных кодов** (папки с `Makefile`).
*   `firmware_output/` — Склад готовых прошивок.
    *   `imagebuilder/` — результаты быстрой сборки.
    *   `sourcebuilder/` — результаты компиляции (включая buildinfo, manifest и json).

---

## ⚙️ Универсальный Профиль

Теперь один файл конфигурирует оба режима сборки. Пример идеального профиля (`profiles/universal_example.conf`):

```bash
# === ОБЩИЕ НАСТРОЙКИ ===
PROFILE_NAME="my_router_universal"

# Общий список пакетов (чтобы не писать дважды)
COMMON_LIST="luci uhttpd openssh-sftp-server htop -dnsmasq dnsmasq-full"

# ==========================================
# ЧАСТЬ 1: IMAGE BUILDER (Быстрая сборка)
# ==========================================
# Ссылка на архив ImageBuilder с сайта OpenWrt
IMAGEBUILDER_URL="https://downloads.openwrt.org/releases/24.10.0/targets/ramips/mt7621/openwrt-imagebuilder-..."
TARGET_PROFILE="xiaomi_mi-router-4a-gigabit"
PKGS="$COMMON_LIST"  # Используем общий список
# EXTRA_IMAGE_NAME="fast-v1"

# ==========================================
# ЧАСТЬ 2: SOURCE BUILDER (Полная компиляция)
# ==========================================
SRC_REPO="https://github.com/openwrt/openwrt.git"
# Рекомендуется использовать ТЕГ (v24.10.0) для совпадения версий ядра (Vermagic)
SRC_BRANCH="v24.10.0"

# Параметры архитектуры (из OpenWrt Table of Hardware)
SRC_TARGET="ramips"
SRC_SUBTARGET="mt7621"
# Имя устройства в menuconfig
SRC_DEVICE="xiaomi_mi-router-4a-gigabit" 

SRC_PACKAGES="$COMMON_LIST" # Используем тот же список

#Добавляем кастомные репозитории [*не обязательно]
#Сначала ключ от репы
CUSTOM_KEYS="https://fantastic-packages.github.io/releases/24.10/53ff2b6672243d28.pub"
#Теперь список репозиториев по одному на строке.
CUSTOM_REPOS="src/gz fantastic_luci https://fantastic-packages.github.io/releases/24.10/packages/mipsel_24kc/luci
src/gz fantastic_packages https://fantastic-packages.github.io/releases/24.10/packages/mipsel_24kc/packages
src/gz fantastic_special https://fantastic-packages.github.io/releases/24.10/packages/mipsel_24kc/special"

# Опции размеров разделов (только для SourceBuilder)
# ROOTFS_SIZE="512"
# KERNEL_SIZE="64"
```

---

## 🏭 Особенности Source Builder

Сборка из исходников — мощный процесс. Вот что нужно знать:

1.  **Кэширование (Persistence):**
    *   Билдер создает отдельные Docker-тома для каждого профиля (`src-workdir`).
    *   **Первый запуск:** Скачиваются исходники, компилируется Toolchain (GCC), собираются инструменты. *Время: 20-60 мин.*
    *   **Второй запуск:** Система видит готовый Toolchain и пересобирает только измененные пакеты. *Время: 2-5 мин.*

2.  **Защита от сбоев:**
    *   Если сборка упала (например, пропал свет), кэш сохраняется. При перезапуске процесс продолжится с того же места.
    *   Автоматическое переключение зеркал: Если `git.openwrt.org` недоступен (ошибка 503), скрипт сам переключит фиды на GitHub.

3.  **Ресурсы:**
    *   В режиме Source Builder задействуются все ядра процессора (`100% CPU`).

---

## 🛠 Дополнительные инструменты

### Генерация профиля
Добавлен `create_profile.ps1` (запускать через `_run_create_profile.bat`). Он парсит сайт OpenWrt и помогает создать заготовку профиля.

### Полезные скрипты (папка `scripts`)

*   `scripts\diag.sh` — Скрипт для диагностики, чтобы отдать данные в нейросеть и получить ответы. Собирает максимум данных о системе, но **скрывает все пароли**.
*   `scripts\packager.sh` — Вспомогательный скрипт. Создаёт в папке `/etc/config` файлы `status` и `status_lite` с полным и сокращенным списком пакетов этой системы.
*   `scripts\upgrade.sh` — Обновлялка всех пакетов, аналог `apt upgrade` в Debian.

---

## ❓ FAQ (Частые вопросы)

***Q: Возможно ли в этом инструменте изменять размер свободного места? Например, сделать 200Мб вместо 100Мб?**
**A:** Да. Начиная с версии 4 инструмента, в профиль можно добавлять параметр `ROOTFS_SIZE=""`.
**Q: ImageBuilder или SourceBuilder? Что выбрать?**
**A:**
*   Если нужно просто добавить плагины LuCI и настроить Wi-Fi — используйте **ImageBuilder**.
*   Если нужно изменить размер ядра, вырезать лишние драйверы для экономии места (на 4MB роутерах) или собрать пакет, которого нет в репозитории — используйте **SourceBuilder**.

***Q: При попытке заменить пакет `dnsmasq` на `dnsmasq-full` выдаёт ошибку. Как исправить?**
**A:** Проблема связана с зависимостями полных версий пакетов.
Методика решения:
1. В готовой прошивке (можно открыть через 7zip) в пути `/usr/lib/opkg/status` посмотрите список пакетов.
2. Если под пакетом написано `autoinstalled: yes` — значит, какой-то другой пакет затребовал именно его.
3. Нужно найти "виновника" и либо поставить только нужные его части, либо явно удалить конфликт.
4. Но начать с простого - в списке пакетов профиля (`PKGS`) укажите удаление стандартного пакета через минус (`-dnsmasq`), а затем добавьте нужный (`dnsmasq-full`).
**Q: Source Builder долго висит на `make[3] ... compile` во второй раз.**
**A:** Это нормально. Система `make` должна пробежаться по тысячам файлов, чтобы убедиться, что они "свежие" и пересобирать их не нужно. Это занимает 1-2 минуты, но тяжелой компиляции не происходит.

**Q: Как добавить свою программу в Source Builder?**
**A:** Найдите исходный код программы (папку с `Makefile`) и положите её в папку `src_packages`. Скрипт автоматически внедрит её в дерево сборки. Не забудьте добавить имя пакета в `SRC_PACKAGES`.

**Q: Ошибка `Image too big` на старом роутере.**
**A:** В профиле ImageBuilder попробуйте убрать лишнее (через минус: `-luci-proto-ipv6`). В SourceBuilder можно выиграть больше места, отключив отладку ядра, но это требует правки конфига (пока в разработке).