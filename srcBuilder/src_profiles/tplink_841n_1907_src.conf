# === Source Profile for TL-WR841N v9 (Legacy 19.07) ===
PROFILE_NAME="841n_1907_src"

# Repo Settings
# Используем тег v19.07.10 для стабильности
SRC_REPO="https://github.com/openwrt/openwrt.git"
SRC_BRANCH="v19.07.10"

# Target Settings (Legacy ar71xx architecture)
SRC_TARGET="ar71xx"
SRC_SUBTARGET="tiny"
# В 19.07 для tiny имя устройства выглядело так
SRC_DEVICE="tl-wr841-v9"

# Packages
# Копируем ваш список один-в-один.
# Важно: для 4MB флешки каждый байт на счету.
SRC_PACKAGES="luci-base luci-mod-admin-full luci-theme-bootstrap luci-i18n-base-ru luci-i18n-opkg-ru \
uhttpd luci-app-firewall libiwinfo-lua relayd luci-proto-relay iw rpcd-mod-rrdns opkg \
-luci -iw-full -luci-proto-ipv6 -luci-proto-ppp -ipv6 -kmod-ipv6 -ip6tables -kmod-ip6tables -odhcp6c -odhcpd-ipv6only -libip6tc \
-ppp -ppp-mod-pppoe -kmod-ppp -kmod-pppoe -kmod-pppox -kmod-slhc -kmod-lib-crc-ccitt \
-luci-app-ntpc -luci-i18n-ntpc-ru -ntpclient \
-libpthread -librt -uboot-envtools -kmod-nf-conntrack6 \
-kmod-usb-core -kmod-usb2"

# Extra config options
# Оставляем пустыми, стандартные размеры
SRC_ROOTFS_SIZE=""
SRC_KERNEL_SIZE=""