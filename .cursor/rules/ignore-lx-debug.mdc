---
description: Ignore lx/ debug unpacked tree (do not read or edit)
alwaysApply: true
---

# Ignore lx/ debug folder

- **Назначение**: Папка `lx/` используется только как временная зона отладки — туда распаковывается дистрибутив (`_unpacker`) для имитации действий конечного пользователя.
- **Требование**: 
  - Не читать, не анализировать и не редактировать файлы внутри `lx/` и любых её вложенных подкаталогов.
  - Всегда считать исходным и «истинным» проектом корень репозитория вне `lx/`.
  - Любые правки и поиск багов должны выполняться только в оригинальных файлах, а не в копиях из `lx/`.

