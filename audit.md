# Полный Аудит Проекта "routerFW"

**Дата аудита:** 2026-01-02

**Цель:** Провести всесторонний анализ проекта `routerFW` для выявления сильных и слабых сторон, а также для выработки рекомендаций по улучшению.

---

## 1. Архитектура

### 1.1. Структура и компоненты

Проект представляет собой **универсальный конвейер для сборки прошивок OpenWrt**, построенный на базе Docker и управляемый через Windows Batch скрипты. Архитектура четко разделена на два режима:

1.  **Image Builder (Быстрая сборка):** Использует официальный SDK OpenWrt для перепаковки готовых бинарных пакетов. Идеально для кастомизации без изменения исходного кода.
2.  **Source Builder (Полная компиляция):** Собирает прошивку с нуля из исходного кода, что дает полный контроль над ядром, драйверами и конфигурацией.

**Ключевые компоненты:**

*   **`_Builder.bat`:** Центральный скрипт-оркестратор, предоставляющий пользователю CLI-меню для выбора режима, профиля и выполнения операций.
*   **`_packer.bat` / `_unpacker.bat`:** Система дистрибуции. `_packer.bat` кодирует все необходимые файлы проекта в Base64 и встраивает их в один самораспаковывающийся скрипт `_unpacker.bat`. Это **очень удачное и элегантное решение** для распространения проекта.
*   **Docker (`docker-compose.yaml`, `docker-compose-src.yaml`):** Обеспечивает полную изоляцию сборочной среды, решая проблемы с зависимостями и "загрязнением" хост-системы.
*   **Профили (`profiles/*.conf`):** Файлы конфигурации, определяющие параметры сборки (версию, таргет, список пакетов) для каждого устройства.
*   **Кастомизация (`custom_files`, `custom_packages`, `src_packages`):** Продуманная структура папок для добавления пользовательских файлов, пакетов и исходников.

### 1.2. Зависимости и окружение

*   **Основная зависимость:** Docker Desktop для Windows.
*   **Сборочная среда:** Контейнеры на базе Ubuntu 22.04 (для современных сборок) и 18.04 (для легаси), что обеспечивает консистентность.
*   **Скрипты:** Windows Batch и PowerShell для хоста; Bash/shell для внутренней логики в контейнерах.

### 1.3. Слабые места и риски

*   **Сложность Batch-скриптов:** Основной управляющий скрипт `_Builder.bat` является очень сложным для сопровождения. Использование продвинутых техник `enabledelayedexpansion` и `goto` для имитации функций делает код трудным для чтения и отладки.
*   **"Магия" в контейнере:** `docker-compose.yaml` запускает длинный и сложный `command` в виде одной строки. Отладка этого скрипта требует запуска контейнера в интерактивном режиме и ручного выполнения команд.
*   **Отсутствие версионирования схемы профилей:** Если в будущем изменится формат `.conf` файлов, старые профили пользователей могут перестать работать без очевидной причины.

---

## 2. Кодовая база

### 2.1. Стиль и консистентность

*   **Batch:** Код в `_Builder.bat` хорошо структурирован с использованием комментариев, но его сложность превосходит возможности языка.
*   **Shell:** Скрипты внутри Docker (в `docker-compose.yaml`) написаны в хорошем стиле, с проверками ошибок, логированием и даже retry-логикой для сетевых операций.
*   **PowerShell (`create_profile.ps1`):** Скрипт для создания профилей хорошо написан, интерактивен и использует API для получения данных, что является лучшей практикой.

### 2.2. Покрытие тестами

*   **Отсутствует.** В проекте нет автоматизированных тестов ни для одного из компонентов. Тестирование, по-видимому, производится вручную путем запуска сборок. Это **ключевой риск** при внесении изменений.

### 2.3. Сложность кода

*   **Очень высокая** для Batch-скриптов. `_Builder.bat` (более 500 строк) находится на пределе поддерживаемости.
*   **Высокая** для Shell-скриптов внутри Docker. Они содержат много логики и ветвлений.
*   **Средняя** для PowerShell и `.conf` файлов.

---

## 3. Производительность

### 3.1. Узкие места

*   **Первая сборка из исходников:** Компиляция Toolchain в режиме `Source Builder` является самым долгим процессом (20-60 минут). Это ожидаемое поведение.
*   **Дисковое пространство:** Кэши Docker (особенно `src-workdir` и `ccache`) могут занимать значительное место на диске.

### 3.2. Оптимизация

Проект **отлично оптимизирован** для ускорения повторных сборок:

*   **`ccache`:** В `Source Builder` используется для кэширования результатов компиляции, что сокращает время повторной сборки до нескольких минут.
*   **Docker Layer Caching:** Docker-образы собираются эффективно.
*   **Именованные тома (Named Volumes):** Кэши SDK, IPK-пакетов и исходников вынесены в Docker Volumes, что обеспечивает их персистентность между запусками.
*   **Параллельная сборка:** Возможность запускать несколько сборок одновременно (`[A] ALL`) — это мощная функция, хорошо реализованная за счет изоляции проектов Docker.

---

## 4. Безопасность

### 4.1. Уязвимости и риски

*   **Целостность дистрибутива:** `_unpacker.bat` содержит Base64-кодированные файлы. Если репозиторий GitHub или канал скачивания будут скомпрометированы, злоумышленник может внедрить вредоносный код в любой из этих файлов. **Отсутствие проверки контрольных сумм** при распаковке является серьезным упущением.
*   **Загрузка без проверки:** Скрипты скачивают SDK и другие артефакты из интернета по HTTPS, но не проверяют их контрольные суммы (checksums) после загрузки. Это создает риск атаки "человек посередине" (MITM) или использования скомпрометированного зеркала.
*   **Выполнение `hooks.sh`:** Скрипт `hooks.sh` выполняется с полными правами внутри контейнера. Если пользователь скачает профиль с вредоносным хуком, это может привести к утечке данных или другим нежелательным последствиям.

### 4.2. Лучшие практики

*   **Изоляция:** Использование Docker — это огромный плюс для безопасности, так как сборочная среда изолирована от хост-системы.
*   **Санитизация в `diag.sh`:** Скрипт диагностики пытается скрыть пароли и ключи, что является хорошей практикой.

---

## 5. Документация

### 5.1. Полнота и актуальность

*   **`README.md`:** Документация **превосходная**. Она очень подробная, хорошо структурирована, ориентирована на пользователя и покрывает все основные аспекты проекта.
*   **`GEMINI.md`:** Хорошее дополнение, дающее более технический обзор, полезный для ИИ-ассистентов или новых разработчиков.
*   **Комментарии в коде:** Код, особенно в `.conf` файлах и `hooks.sh`, хорошо прокомментирован, что объясняет "зачем" и "как".

---

## 6. Итоги и рекомендации

**Сильные стороны:**
*   👍 **Элегантная система дистрибуции:** `_unpacker.bat` — это гениальное решение.
*   👍 **Мощный функционал:** Поддержка двух режимов сборки, кастомизации и хуков.
*   👍 **Отличная производительность:** Продвинутые механизмы кэширования.
*   👍 **Превосходная документация:** `README` является образцовым.
*   👍 **Хороший UX:** Интерактивное меню и мастер создания профилей.

**Зоны для улучшения:**
*   🚨 **Безопасность:** Необходимо внедрить проверку контрольных сумм для скачиваемых артефактов и для файлов внутри `_unpacker.bat`.
*   📉 **Сопровождаемость:** `_Builder.bat` критически нуждается в рефакторинге. Переход на PowerShell или Python для основной логики значительно упростит поддержку.
*   🧪 **Тестирование:** Отсутствие тестов — это бомба замедленного действия. Стоит начать с простых скриптов для проверки базовых сценариев сборки.
*   🏗️ **Упрощение `docker-compose`:** Длинные shell-скрипты внутри `command` стоит вынести в отдельные `.sh` файлы и копировать их в образ, чтобы упростить чтение и отладку.

### Краткосрочные рекомендации:

1.  **Добавить проверку SHA256:** В `_packer.bat` генерировать файл с контрольными суммами всех упаковываемых файлов. В `_unpacker.bat` — проверять их перед записью на диск.
2.  **Вынести скрипты из `docker-compose.yaml`:** Перенести длинные команды из `command:` в отдельные `entrypoint.sh` файлы.
3.  **Добавить версионирование профилей:** В `.conf` файлы добавить поле типа `PROFILE_VERSION=1`, чтобы в будущем можно было обрабатывать старые форматы.

### Долгосрочные рекомендации:

1.  **Рефакторинг `_Builder.bat`:** Полностью переписать управляющую логику на PowerShell. Это основной технический долг проекта.
2.  **Внедрить CI/CD:** Настроить GitHub Actions для автоматического запуска сборок по тестовым профилям при каждом коммите, чтобы ловить регрессии.
